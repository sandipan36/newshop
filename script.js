// JavaScript function to handle the search functionality
function searched() {
    const searchTerm = document.getElementById("sear-txt").value;
    if (searchTerm.trim() !== "") {
      // Replace this alert with your desired action, e.g., redirect to search results page.
      alert("You searched for: " + searchTerm);
    }
  }